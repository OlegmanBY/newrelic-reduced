{
  "name": "@my-sale/newrelic-reduced",
  "version": "1.1.6",
  "description": "NewRelic plugin that can be disabled for some percent of users. Cut your costs!",
  "main": "lib/index.js",
  "files": [
    "lib"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "gulp dev",
    "build": "gulp build",
    "package": "npm run build && npm pack"
  },
  "devDependencies": {
    "gulp": "^4.0.0",
    "gulp-connect": "^5.5.0",
    "gulp-html-replace": "^1.6.2",
    "gulp-rename": "^1.3.0",
    "gulp-uglify": "^3.0.0",
    "pump": "^3.0.0"
  },
  "dependencies": {}
}
